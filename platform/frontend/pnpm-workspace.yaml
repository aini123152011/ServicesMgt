# pnpm 构建脚本审批：esbuild 需要执行安装脚本，clerk 模板依赖不执行（未使用 Clerk）
onlyBuiltDependencies:
  - esbuild
ignoredBuiltDependencies:
  - '@clerk/shared'
# pnpm 12 的构建审批开关：不显式置位时 install 直接失败（ERR_PNPM_IGNORED_BUILDS）
allowBuilds:
  '@clerk/shared': false
  esbuild: true
