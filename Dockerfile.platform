FROM python:3.14
# 平台版本与构建号：构建时注入（--build-arg APP_VERSION=x.y.z），
# 由后端 /api/v1/system/info 暴露，供界面显示与更新后核对
# NOTE: 构建参数不能叫 PLATFORM_VERSION——Docker 里同名 ARG 与 ENV 会互相遮蔽，
# 上一层已 ENV PLATFORM_VERSION，同名 ARG 的取值会被 ENV 覆盖，--build-arg 静默失效
ARG APP_VERSION=dev
ARG APP_BUILD=
ENV PYTHONUNBUFFERED=1 \
    PLATFORM_VERSION=${APP_VERSION} \
    PLATFORM_BUILD=${APP_BUILD}
ENV PIP_INDEX_URL=https://mirrors.aliyun.com/pypi/simple/
ENV UV_HTTP_TIMEOUT=60
RUN pip install --no-cache-dir uv
ENV UV_COMPILE_BYTECODE=1
ENV UV_LINK_MODE=copy
ENV UV_DEFAULT_INDEX=https://mirrors.aliyun.com/pypi/simple/

COPY platform/backend/uv.lock platform/backend/pyproject.toml /app/backend/
WORKDIR /app/backend/
ENV PATH=/app/backend/.venv/bin:$PATH
RUN uv sync --frozen --no-install-workspace --package app

COPY platform/backend/scripts /app/backend/scripts
COPY platform/backend/alembic.ini /app/backend/
COPY platform/backend/app /app/backend/app
COPY platform/frontend/dist /app/backend/app/frontend
RUN uv sync --frozen --package app

EXPOSE 8000

# 判活依据：健康检查端点返回 200 即视为可用。
# 用 python 而不是 curl：基础镜像里没有 curl，装它只为健康检查不值当。
# NOTE: 平台自更新的「健康检查失败自动回滚」依赖这条——容器没有 HEALTHCHECK 时
# Docker 不会给出 healthy，重建器只能看到 Running 就判成功，回滚形同虚设（复核 P1）
HEALTHCHECK --interval=15s --timeout=5s --start-period=30s --retries=5     CMD python -c "import urllib.request;urllib.request.urlopen('http://127.0.0.1:8000/api/v1/utils/health-check/', timeout=4)" || exit 1

CMD ["fastapi", "run", "--workers", "2"]
