{#- 平台渲染契约：后端按 schema.json 校验用户提交的配置值后，用本模板渲染出 faketime.conf 写入配置卷 -#}
{#- 本文件不参与 chronyd 自身配置，只作为 entrypoint 与 reload.sh 之间的偏移量传递文件 -#}
{#- 渲染环境要求：Jinja2 需开启 trim_blocks=True + lstrip_blocks=True（Ansible 同款默认），否则块标签会残留空行 -#}
# 本文件由 BMC 服务管理平台自动渲染生成，请勿手工编辑
# 用途：entrypoint 读取 FAKETIME_OFFSET 决定是否用 libfaketime 启动 chronyd；
#       偏移量在启动时通过 LD_PRELOAD 注入，无法热加载，变更需重启 chronyd 进程（见 /reload.sh）
# 取值：相对偏移（如 +3h、-30d）或绝对日期（如 2038-01-01）；空值表示不注入
{% if fault_mode == 'fake_offset' %}
FAKETIME_OFFSET={{ fake_time_offset }}
{% else %}
FAKETIME_OFFSET=
{% endif %}
