# 仓库根忽略规则（各子项目另有局部 .gitignore）

# 环境变量（含密钥，禁止入库）
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# Node / 前端构建产物
node_modules/
dist/
coverage/

# 运行时产物
*.log
*.pid

# 部署打包产物（部署流程生成的 tar 包，不入库）
deploy-*.tar.gz

# 编辑器与系统文件
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# --------------------------------------------------------------------------- #
# Trellis 相关文件：不进版本控制、也不上传 GitHub（方案 B）
# 说明：本机保留这些文件以便 Trellis 继续工作；仓库级不保留任何副本与备份。
# 只加忽略是不够的——历史提交里仍会存在，必须配合一次性历史重写（见任务
# 09-22-github-only-publish 的 design.md）。
# --------------------------------------------------------------------------- #
.trellis/
.agents/
.claude/
.zcode/
.trae/
.codex/
AGENTS.md
# 工具生成的忽略文件快照（ZCode 会按 .gitignore 生成，不是项目内容）
.zcodeignore
